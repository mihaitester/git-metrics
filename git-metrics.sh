#!/bin/sh

# git show <commit> --name-status
# todo: figure out how to get the commits that are closest to input dates
# git diff <commit1> <commit2> --name-status | grep "A" - get added files - new code
# git diff <commit1> <commit2> --name-status | grep "M" - get modified files - maintenance
